import React from 'react';

const styles = {
  wrapper: {
    marginBottom: '20px'
  }
};

const Loading = () => {
  return (
    <div style={styles.wrapper}>loading...</div>
  );
};

export default Loading;
